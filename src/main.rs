use vkfetch_rs::iterate_devices;
fn main() {
    // let vendor = Vendor::Unknown;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));
    // let vendor = Vendor::Apple;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));
    // let vendor = Vendor::Google;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));
    // let vendor = Vendor::Intel;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));
    // let vendor = Vendor::Nvidia;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));
    // let vendor = Vendor::AMD;
    // let x = vendor.get_ascii_art();
    // println!("{}", x.join("\n"));

    iterate_devices()
}
